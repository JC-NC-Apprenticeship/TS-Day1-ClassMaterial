/* // Task
let rad: unknown = 4;

const calculateAreaOfCircle = (radius: unknown) => {
  const pi: unknown = 3.14;
  return pi * (radius * radius);
};
calculateAreaOfCircle(rad);
*/

let rad: number = 4;

const calculateAreaOfCircle = (radius: number) => {
  const pi: number = 3.14;
  return pi * (radius * radius);
};
calculateAreaOfCircle(rad);