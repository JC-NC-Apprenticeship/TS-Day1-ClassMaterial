/* // Task
let price: unknown = 100;

const isExpensive = (val: unknown) => {
  return val > 50;
};
isExpensive(price);
*/

let price: number = 100;

const isExpensive = (val: number) => {
  return val > 50;
};
isExpensive(price);